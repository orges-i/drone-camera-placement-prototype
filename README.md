
  # Drone Camera Placement Tool

  This is a code bundle for Drone Camera Placement Tool. The original project is available at https://www.figma.com/design/sPOh5d7Va6dmZyTBqLWoid/Drone-Camera-Placement-Tool.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  